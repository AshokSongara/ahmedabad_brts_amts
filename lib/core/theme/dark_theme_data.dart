import 'package:flutter/material.dart';

import '../../utils/app_colors.dart';

var darkThemeData = ThemeData(
    brightness: Brightness.dark,
    //unselectedWidgetColor: AppColors.lightGray,
   // selectedRowColor: AppColors.lightGray
);
